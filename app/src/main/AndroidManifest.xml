<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quantumsit.sportsinc">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- FOR SMS verification -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".Aaa_data.GlobalVars"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:theme="@style/AppTheme.ActionBar.Transparent" />
        <activity
            android:name=".Activities.HomeActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.HomeTabsActivity" />
        <activity android:name=".Activities.MyClasses_scoresActivity" />
        <activity android:name=".Activities.Reports_coursesActivity" />
        <activity android:name=".Activities.Request_addActivity" />
        <activity android:name=".COACHES.ActivityAddRequest_coach" />
        <activity android:name=".COACHES.ActivityCurrentClass_coach" />
        <activity android:name=".COACHES.ActivityCourseSingleClass_coach" />
        <activity android:name=".Activities.ClassesDetailsActivity" />
        <activity android:name=".COACHES.ActivityFinishedCourseSingle_coach" />
        <activity
            android:name=".Activities.ProfileActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".Activities.CourseDetailsActivity" />
        <activity android:name=".Activities.EventsDetailsActivity" />
        <activity android:name=".Activities.NewsDetailsActivity" />
        <!-- activity android:name=".CourseDetailsActivity" / -->
        <activity android:name=".Activities.NotificationDetailsActivity" />
        <activity android:name=".ADMINS.AdminStartClassActivity" />
        <activity android:name=".Activities.NewsActivity" />
        <activity android:name=".Activities.EventsActivity" />
        <activity android:name=".Activities.RequestDetailsActivity" />
        <activity android:name=".Activities.ComplainDetailsActivity" />
        <activity android:name=".Activities.ComplainsAddActivity" />

        <!--
            The API key for Google Maps-based APIs is defined as a string resource.
            (See the file "res/values/google_maps_api.xml").
            Note that the API key is linked to the encryption key used to sign the APK.
            You need a different API key for each encryption key, including the release key that is used to
            sign the APK for publishing.
            You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!--
            activity
            android:namContactUsFragmentvity"
            android:theme="@style/AppTheme"
            android:label="@string/title_activity_maps" /
        -->


        <!-- Firebase Notifications -->
        <service android:name=".services.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".services.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <!-- ./Firebase Notifications -->
        <activity android:name=".Activities.FirstActivity" />
        <activity android:name=".RegisterationForm_fragments.BookingForm_FirstFragment" />
        <activity android:name=".RegisterationForm_fragments.BookingForm_SecondFragment" />
        <activity android:name=".RegisterationForm_fragments.BookingFormActivity" />
        <activity android:name=".RegisterationForm_fragments.BookingFirstFormActivity"></activity>
    </application>

</manifest>